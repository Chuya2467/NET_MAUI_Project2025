<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Project2025.MainPage"
             Title="Movie Explorer">

    <ScrollView>
        <VerticalStackLayout Padding="20">

            <Entry Text="{Binding LibraryName}" Placeholder="Search movie"/>

            <CollectionView ItemsSource="{Binding Movie}" SelectionMode="Single" SelectedItem="{Binding SelectedAlbum}" SelectionChanged="CollectionView_SelectionChanged">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout Padding="10">
                            <Label Text="{Binding Title}" FontSize="Medium" FontAttributes="Bold"/>
                            <Label Text="{Binding Year}" FontSize="Small"/>
                            <Label Text="{Binding Genre}" FontSize="Small"/>
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
